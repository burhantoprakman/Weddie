<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="homeScreenTextViewStyle">
        <item name="android:textColor">@color/home_screen_text_color</item>
        <item name="android:textStyle">bold</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/home_screen_text_color</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_emoji</item>
        <item name="postSplashScreenTheme">@style/Theme.BiDugunApp</item>
        <item name="windowSplashScreenBackground">@color/white</item>
    </style>
</resources>